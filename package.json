{
  "name": "block-diagram-reducer",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "build": "esbuild app.js --bundle --outfile=bundle.js --format=iife",
    "start": "electron .",
    "package": "electron-packager . BlockDiagramReducer --platform=win32 --arch=x64 --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "katex": "^0.17.0"
  },
  "devDependencies": {
    "electron": "^42.3.0",
    "electron-packager": "^17.1.2",
    "esbuild": "^0.28.0"
  }
}
